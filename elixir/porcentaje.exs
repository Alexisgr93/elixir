a = IO.gets("ingrese el primer valor ")
b = IO.gets("ingrese el segundo valor")
#nombre_limpio = String.trim(nombre)


defmodule Calculadora do
  def suma(a,b) do
    #c= a + b
    #c
    IO.puts a+b
  end
  IO.puts a+b
end
#IO.puts(c)
#IO.puts "hola, #{nombre_limpio}. ¡encantado de conocerte!"
IO.puts a+b
